<!DOCTYPE html>
<html>
  <head>
    <title>Saperi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <head>
  <style>

    @keyframes pulse1 {
      0%, 100% {
        transform: translate( -100%, -0%) scale(.2);
      }
      50% {
        transform: translate( 100%, 0%) scale(1);
      }
    }

    @keyframes pulse2 {
      0%, 100% {
        transform: translate(-0%, -100%) scale(1);
      }
      50% {
        transform: translate(-100%, 0%) scale(.2);
      }
    }

    @keyframes pulse3 {
      0%, 100% {
        transform: translate(00%, -100%) scale(1);
      }
      50% {
        transform: translate( 100%, 50%) scale(.2);
      }
    }
     @keyframes pulse4 {
      0%, 100% {
        transform: translate( -0%, 50%) scale(1);
      }
      50% {
        transform: translate( 50%, -0%) scale(.1);
      }
    }
  </style>
  </head>
  <body>
    <div class="container-fluid ">
      <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">
          <%= image_tag("Sapiam_Logo.png", class: "navbar-brand img-fluid" , alt: "Sapiam Logo") %>
          </a>
           <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" onclick="console.log('Button clicked!')"> 
            <span class="navbar-toggler-icon">
            </span>
          </button>
          <%# target for my collapse button %>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to 'About', about_path, class:"nav-link"%>
              </li>
              <li class="nav-item">
                <%= link_to 'Collections', collections_path, class:"nav-link"%>
              </li>
              <li class="nav-item">
                <%= link_to 'Login', login_path, class: "nav-link"%>
              </li>
            <% if current_user.present? %>
              <%# <li class="nav-item"> %>
              <%# <%= link_to 'New Collection', new_collection_path, class:"nav-link" %> 
              <%# </li> %>
              <%# <li> %>
              <%# <%= link_to 'New source', new_source_path, class:"nav-link" %>  
              <%# </li> %> 
              <%# removed source and collection create links from nav bar and installed buttons in the profile page instead %>
            </ul>
              <li class="d-flex nav-item">
                <%= button_to 'Profile', user_path(@current_user), method: :get, class: "btn btn-outline-primary"%>
              </li>
            <%end %>
          </div>
        </div>
      </nav>
      <%= yield %>
    </div>
  </body>
<br/>
</html>
<footer class="footer mt-auto py-3 bg-light">
    <div class="container d-flex flex-column flex-md-row justify-content-between">
        <div class="text-center">
            <a href="https://www.linkedin.com/in/aotaola/">
                <%= image_tag("Linkedin.png", class: "navbar-brand img-fluid", alt: "Linkedin Logo") %>
            </a>
        </div>
        <div class="text-center text-md-left">
            <span class="text-muted">Built by </span>
            <a href="https://aotaola.netlify.app/">Alejandro Otaola</a>
        </div>
        <div class="text-center">
            <a href="https://github.com/Aotaola">
                <%= image_tag("Github_LOGO.png", class: "navbar-brand img-fluid", alt: "Github Logo") %>
            </a>
        </div>
    </div>
</footer>


